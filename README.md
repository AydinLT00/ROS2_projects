# ROS2_projects

loaded a carter rover in isaacsim environment, the current loaded model is already configured to handle twist messages on /cmd_vel topic to move the robot using differential driver

proj_2 contain the commander package which contain the carter_teleop which sends a twist message to /cmd_vel and therefore move the robot
